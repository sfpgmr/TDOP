type A = i32;
type B = A;
//vec4 a;


type vec2<T>{
  type ThisType = vec2<T>;
  T .[2];
  alias .x = .[0];
  alias .y = .[1];
  alias .u = .x;
  alias .v = .y;
  i32 operator *(
};

operator<T> mul (vec2<T> lhs,vec2<T> rhs)

export i32 main (){
  A b = 1;
  B c = 2;
  return b + c;
};
